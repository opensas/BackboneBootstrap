#check https://www.playframework.com/documentation/2.3.x/Build#The-/build.sbt-file.

name := "webservice"

version := "1.1"

libraryDependencies ++= Seq(
  jdbc,
  anorm
)

lazy val root = (project in file(".")).enablePlugins(PlayScala)
